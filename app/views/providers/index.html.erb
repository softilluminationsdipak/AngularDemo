<h3>
  Providers
  <ol class="breadcrumb pull-right">
    <%= render_breadcrumbs tag: :li, separator: '' %>
  </ol>
  <small>
    Providers supply services and/or products to your patients. Information on this screen is used for printing insurance claims for your patients. <br>
	The reports menu lets you list your providers or generate a productivity report over a specific date range.
  </small>
</h3>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <div class="table-responsive">

          <% if @providers.present? %>
            <table class="table table-hover">
              <thead>
                <tr>
                  <th>#</th>
                  <th>Provider Name</th>
                  <th>Created At</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
              	<% @providers.each_with_index do |provider, index| %>              		
                  <tr class='provider_<%= provider.slug %>'>
              			<td><%= index + 1 %></td>
              			<td><%= provider.signature_name %></td>              			
              			<td><%= provider.created_at.strftime('%a, %d %b %Y - %H:%M %p') %></td>
              			<td>
              				<%= link_to "<em class='fa fa-eye'></em> Show".html_safe, provider_path(provider), class: 'btn btn-default btn-xs', data: {disable_with: 'Please wait...'} %>
                      <%= link_to "<em class='fa fa-pencil'></em> Edit".html_safe, edit_provider_path(provider), class: 'btn btn-primary btn-xs', data: {disable_with: 'Please wait...'} %>
                      <%= link_to "<em class='fa fa-trash'></em> Delete".html_safe, provider_path(provider), class: 'btn btn-danger btn-xs', data: {behavior: 'delete', klass: "provider_#{provider.slug}"} %>
              			</td>
              		</tr>
              	<% end %>
							</tbody>
						</table>
					<% else %>
						No Provider Found.
					<% end %>        	
       	</div>
      </div>
    </div>
  </div>
</div>

