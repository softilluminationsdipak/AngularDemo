<h3> Appointment - <%= @contact.try(:name) %>
  <ol class="breadcrumb pull-right">
    <%= render_breadcrumbs tag: :li, separator: '' %>
  </ol>
  <small>
  	The appointment book allows scheduling the current patient to the particular time for one of the doctors.
  </small>
</h3>

<div class="panel-body">
  <div class="panel panel-default">
    <div class="panel-body">
    	<%= form_tag day_at_glance_clinic_patient_appointments_path(@clinic, @patient), html: {class: 'form-inline'} do |form| %>
    		<div class="form-group col-lg-2">
	    		<label for="input-date" class="sr-only">Date</label>
	        <div class="input-group date" id="datepicker1">
	          <%= text_field_tag :date, @date.strftime('%d/%m/%Y'), class: "calendar datepicker form-control", 'dateFormat' => '%d/%m/%Y', placeholder: 'Date' %>
	          <span class="input-group-addon">
	            <span class="fa fa-calendar"></span>
	          </span>
	        </div>
	        <%= hidden_field_tag :contact_id, params[:contact_id] %>
    		</div>
		    <div class="form-group col-lg-4">
	    		<%= link_to "<i class='fa fa-plus'></i> Add".html_safe, '#', class: 'btn btn-default' %>
  	  		<%= link_to "<i class='fa fa-trash'></i> Delete".html_safe, '#', class: 'btn btn-danger' %>		    
  	  	</div>
  	  	<div class="form-group col-lg-6 text-right">
		    	<%= link_to "<i class='fa fa-calendar'></i> Day At Glance".html_safe, '#', class: 'btn btn-default btn-sm' %>
	  	  	<%= link_to "<i class='fa fa-calendar'></i> Week At Glance".html_safe, '#', class: 'btn btn-default btn-sm' %>
	    		<%= link_to "<i class='fa fa-bed'></i> Room".html_safe, rooms_path, class: 'btn btn-default btn-sm' %>
	  		</div>
    	<% end %>
	  </div>
  </div>

  <div class="panel panel-default">
    <div class="panel-body">
    	
    </div>
  </div>
</div>
